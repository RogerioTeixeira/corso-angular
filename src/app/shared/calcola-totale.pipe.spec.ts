import { CalcolaTotalePipe } from './calcola-totale.pipe';

describe('CalcolaTotalePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcolaTotalePipe();
    expect(pipe).toBeTruthy();
  });
});
